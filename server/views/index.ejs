<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/main.css" rel="stylesheet">
</head>
<body ng-app="BAI">
    <div class="container">
        <header class="row">
            <h1><%= title %></h1>
        </header>
        <article class="row">
            <ui-view></ui-view>
        </article>
        <footer class="row">
            <small>Made with AngularJS</small>
        </footer>
    </div>
    <script type="text/ng-template" id="/index.html">
        <section ng-controller="IndexController as indexController">
            <h2>Inventories</h2>
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th class="text-center">Date</th>
                        <th class="text-center">Score</th>
                        <th class="text-center">Degree</th>
                        <th colspan=2>&nbsp;</th>
                    </tr>
                </thead>
                <tbody>
                    <tr ng-repeat="inventory in indexController.inventories">
                        <td class="text-center">
                            {{indexController.formatDate(inventory.date)}}
                        </td>
                        <td class="text-center">
                            {{inventory.score}}
                        </td>
                        <td class="text-center">
                            {{inventory.degree}}
                        </td>
                        <td class="text-center">
                            <a class="btn btn-primary btn-sm" href="#/inventories/{{inventory._id}}" ng-click="indexController.showInventory(inventory)">Show</a>
                        </td>
                        <td class="text-center">
                            <button class="btn btn-default btn-sm" ng-click="indexController.deleteInventory(inventory)">Delete</button>
                        </td>
                    </tr>
                </tbody>
            </table>
        </section>
    </script>
    <script type="text/ng-template" id="/show.html">
        <h2>Inventory</h2>
        <p>Rate how much each symptom has bothered you during the past week.</p>
    </script>
    <script type="text/javascript" src="/js/lodash.js"></script>
    <script type="text/javascript" src="/js/angular.js"></script>
    <script type="text/javascript" src="//cdnjs.cloudflare.com/ajax/libs/angular-ui-router/0.2.10/angular-ui-router.js"></script>
    <script type="text/javascript" src="/js/moment.js"></script>
    <script type="text/javascript" src="/js/angular-wizard.js"></script>
    <script type="text/javascript" src="/js/main.js"></script>
</body>
</html>
